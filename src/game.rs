
use tiny;

const MAP_WIDTH: usize = 80;
const MAP_HEIGHT: usize = 60;

struct Pos(u32, u32);

struct Unit {

}

struct Cell {

}

struct Map {
	cells: [Cell; MAP_WIDTH * MAP_HEIGHT],
}

struct Game {

}